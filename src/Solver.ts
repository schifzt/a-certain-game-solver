class Solver {
    wordlist: string[];

    constructor(wordlist: string[]) {
        this.wordlist = wordlist;
    }
    solve(): string {
        let guess: string = "apple";
        return guess;
    }

}

module.exports = Solver;

